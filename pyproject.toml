[project]
name = "quant-team"
version = "0.1.0"
description = "A self-improving AI quantitative trading firm powered by Claude Code and Codex CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "requests>=2.32.5",
]
